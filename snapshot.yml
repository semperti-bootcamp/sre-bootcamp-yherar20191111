---

- hosts: all
    
  vars:
    project_path: /home/yherar/sre-bootcamp-yherar-11-11-2019/Code
    snapshot: true

  tasks:
    - name: Creacion de version snapshot
      command: "mvn -f {{ project_path }}/pom.xml versions:set -DnewVersion={{ 9.0 }}-SNAPSHOT"
      
      
    - name: Deploy a nexus
      command: "mvn -f {{ project_path }}/pom.xml clean deploy"
...
 